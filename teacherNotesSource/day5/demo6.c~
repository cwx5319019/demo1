#include<stdio.h>

/*
	计算1到100中所有奇数的和
*/

int main(){
	int i = 0;
	int res = 0;
	int count = 0;
	for(i=1;i<=100;i++){
		if(i%2!=0){
			count++;
		}
	}
//我们把从初始点，到结束点中所有数据，全都访问并且只访问一遍的方式，叫做遍历
	printf("一共有 %d 个奇数\n",count);
	return 0;
}
